rules:
  - id: symbol-weak_credentials-python
    languages: [python]
    message: Patch-aware sink for weak_credentials
    severity: ERROR
    pattern-either:
      - pattern: get_netrc_auth(...)
      - pattern: rebuild_auth(...)
      - pattern: utils(...)
