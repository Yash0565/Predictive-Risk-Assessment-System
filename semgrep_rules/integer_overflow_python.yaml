rules:
  - id: detect-integer_overflow-python
    languages: [python]
    message: Detects integer overflow vulnerability.
    severity: ERROR
    patterns:
      - $PILLOW_INCREASE(...$)
      - $PILLOW_SHRINK(...$)
      - $PILLOW_ROTATE_LEFT(...$)
      - $PILLOW_ROTATE_RIGHT(...$)