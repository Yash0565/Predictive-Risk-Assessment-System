rules:
  - id: detect-timing_attack_python
    pattern-file: timing_attack_pattern.yaml
    languages:
      - python
    description: Detects potential timing attack vulnerabilities in the cryptography package.
    severity: high
    match-text: |
      sensitive data
    meta:
      cwe: ["CWE-385"]
      packages: [cryptography]