# Intentionally outdated pins for bundled vulnerable sample apps (Python <3.11 only).
# On Python 3.11+ these lines are skipped so `pip install -r requirements.txt` stays usable;
# use a Python 3.10 venv if you need this exact legacy stack in the same environment as core.
#
Django==2.1.0; python_version < "3.11"
Flask==0.12; python_version < "3.11"
requests==2.19.0; python_version < "3.11"
urllib3==1.24.1; python_version < "3.11"
PyYAML==5.1; python_version < "3.11"
jinja2==2.10; python_version < "3.11"
pillow==6.0.0; python_version < "3.11"
cryptography==2.3; python_version < "3.11"
